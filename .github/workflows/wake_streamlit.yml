name: Wake Streamlit App

on:
  schedule:
  - cron: '0 */4 * * *'
  workflow_dispatch:

jobs:
  wake:
    runs-on: ubuntu-latest
    steps:

      - name: Wake Streamlit
        run: |
          echo "Waking Streamlit app..."
          curl -L -s https://mercadopago-donelio.streamlit.app/ || true
          echo "Done"
